{% extends 'base.html.twig' %}

{% block title %}FindPlace{% endblock %}

{% block body %}
    <h1>FindPlace</h1>

    <table class="table">
     <thead>
            <tr>
                <th>Id</th>
                <th>Артефакт</th>
                <th>Место раскопок</th>
            </tr>
        </thead>
        <tbody>
            <tr>
               
                <td>{{ find_place.id }}</td>
             
                <td>{{ find_place.artName }}</td>
               
                <td>{{ find_place.place }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_find_place_index') }}">back to list</a>

    <a href="{{ path('app_find_place_edit', {'id': find_place.id}) }}">edit</a>

    {{ include('find_place/_delete_form.html.twig') }}
{% endblock %}
