{% extends 'base.html.twig' %}

{% block title %}Arh{% endblock %}

{% block body %}
    <h1>Arh</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arh.id }}</td>
            </tr>
            <tr>
                <th>ArhName</th>
                <td>{{ arh.arhName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_arh_index') }}">back to list</a>

    <a href="{{ path('app_arh_edit', {'id': arh.id}) }}">edit</a>

    {{ include('arh/_delete_form.html.twig') }}
{% endblock %}
