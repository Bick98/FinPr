<form method="post" action="{{ path('app_find_place_delete', {'id': find_place.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ find_place.id) }}">
    <button class="btn">Delete</button>
</form>
